---author: "Michael Henderson"
date: 2026-06-21
linktitle: "100%成功！国内用户免翻墙，智谱清言低代码接入Node.js示例手把手图文教程，新手也能一次跑通"
menu:
  main:
    parent: tutorials
next: /posts/token2
prev: /posts/token1
title: "100%成功！国内用户免翻墙，智谱清言低代码接入Node.js示例手把手图文教程，新手也能一次跑通"
weight: 1
tags: ["AI模型", "ChatGPT"]
---

# 100%成功！国内用户免翻墙，智谱清言低代码接入Node.js示例手把手图文教程，新手也能一次跑通

说实话，国产大模型现在真的很强，像智谱清言的 GLM-4 系列，不管是写代码、做分析还是处理长文本，表现都相当能打。但真到了要把它接到自己项目里那一步，很多国内开发者就卡住了 —— 翻墙、绑卡、配环境，一套折腾下来，热情先凉了一半。

最近我发现了一个特别省事的解法，用[千聚api聚合站](https://www.qianjuai.com/)（www.qianjuai.com）的 API 接口来调用智谱清言的模型。整个过程真的就是改一行代码的事，不用翻墙，不用绑海外卡，从零开始一步步操作，甚至不需要你会复杂的后端配置。今天我就把整个接入流程掰开揉碎了写出来，保你跟着跑完，代码就能跑通。



---

### 为什么选[千聚api聚合站](https://www.qianjuai.com/)来对接智谱清言

在聊具体代码之前，先说清楚为什么选这个方案。[千聚api聚合站](https://www.qianjuai.com/)是一个国内可直连的 AI 大模型 API 中转平台，它做的事情很简单：让你在国内网络环境下，直接用标准的 OpenAI 接口格式调用各种主流模型，包括智谱清言的 GLM-4、GLM-4-Plus、GLM-4-9B-Chat 等。

它的接口完全兼容 OpenAI 标准，这意味着你之前用 OpenAI 写过的任何 Node.js 代码，只需要改两行配置就能接上智谱清言。最关键的是 **完全免翻墙** ，注册也只需要国内邮箱，一块钱就能充进去试。

👉 [立即注册千聚api聚合站](https://www.qianjuai.com/register)

---

### 低代码接入到底有多低代码

我说的“低代码”，其实指的是 **极简配置 + 无需改代码逻辑** 。你不需要去学智谱清言自家的 SDK 格式，也不用理解什么复杂的鉴权机制。只要你写过一个 OpenAI 的 Node.js demo，整个过程就变成了“找两个地方，改一行字”：

1. 把 `base_url` 换成 `https://www.qianjuai.com/v1` 
2. 把 API key 换成[千聚api聚合站](https://www.qianjuai.com/)生成的 key

就这么简单。下面我带你从零走一遍，图文并茂，新手也能一次跑通。

---

### 第一步：注册[千聚api聚合站](https://www.qianjuai.com/)并获取 API Key

打开 [千聚api聚合站官网](www.qianjuai.com)，用你的国内邮箱注册一个账户。注册后，进入控制台，你会看到一个“API 密钥管理”模块。

1. 点击“创建新密钥”，系统会生成一串以 `sk-` 开头的 key。
2. 把这个 key 复制下来，存好，后面代码里要用到。
3. 新用户会自动获得 **$0.2 的免费额度** ，用来试用智谱清言的模型完全够跑十几个请求。

整个过程不需要绑信用卡，不需要海外手机号，两分钟搞定。

👉 [注册领取免费额度](https://www.qianjuai.com/register)

---

### 第二步：创建 Node.js 项目并安装依赖

假设你电脑上已经装好了 Node.js（版本 18+ 最好），我们直接在本地操作。

打开终端，创建一个新文件夹并进入：

bash
mkdir my-glm4-app
cd my-glm4-app


初始化项目：

bash
npm init -y


安装 `openai` 库，这个是 OpenAI 官方 Node.js 包，但因为接口兼容，我们用来调智谱清言也一样好用：

bash
npm install openai


就这么简单，两个命令，依赖装好了。

---

### 第三步：写一个“智谱清言调用”脚本

在项目根目录下新建一个文件，叫做 `test-glm4.mjs`（用 `.mjs` 后缀是为了方便直接用 ES Module 语法），然后写入下面的代码：

javascript
import OpenAI from 'openai';

// 创建客户端实例
const client = new OpenAI({
  baseURL: 'https://www.qianjuai.com/v1', // 关键：改成[千聚api聚合站](https://www.qianjuai.com/)的地址
  apiKey: 'sk-your-api-key-here',         // 替换成你刚才复制的key
});

async function main() {
  const response = await client.chat.completions.create({
    model: 'glm-4-plus',                   // 指定智谱清言的模型
    messages: [
      { role: 'user', content: '用简洁的话解释一下“低代码”是什么意思？' },
    ],
  });

  console.log('智谱清言回复：', response.choices[0].message.content);
}

main();


这段代码做了什么：

- **第 3 行**：导入 OpenAI 库
- **第 6 行**：设置 `baseURL` 为[千聚api聚合站](https://www.qianjuai.com/)的接口地址，**这是最关键的一行**
- **第 7 行**：填入你上面获取的 API key
- **第 10-14 行**：调用 `chat.completions.create`，指定模型为 `glm-4-plus`，传一句用户消息
- **第 16 行**：打印模型返回的内容

**请务必将 `apiKey` 中的占位文本替换为你自己的真实 key。** 如果你想测试其他智谱清言模型，比如 `glm-4`、`glm-4-9b-chat`，把 `model` 字段的值换一下就行。

---

### 第四步：运行脚本，见证奇迹

在终端中执行：

bash
node test-glm4.mjs


如果一切正常，几秒钟后你就会看到类似下面的输出：


智谱清言回复：低代码是一种开发方法，通过可视化拖拽和少量代码来快速构建应用，降低传统编码门槛。


恭喜你！你没翻墙，没用海外信用卡，没有配置任何复杂的代理环境，就用 Node.js 成功调用了智谱清言的大模型。这就是“低代码接入”的真实含义 —— 把最复杂的事情交给[千聚api聚合站](https://www.qianjuai.com/)处理，开发者只需关注业务逻辑。

---

### 价格透明：调用智谱清言贵不贵

[千聚api聚合站](https://www.qianjuai.com/)的计费方式非常清晰：**1 元人民币 = 1 美元 Token 额度**，按模型官方价格 1:1 换算。智谱清言的 GLM-4-Plus 官方每百万输入 Token 定价大约是 0.5 美元，通过[千聚api聚合站](https://www.qianjuai.com/)调用差不多就是 **0.5 元人民币** 就能处理百万 Token 的输入，性价比相当高。

更重要的是，最低 1 元即可充值，没有门槛，不需要预存大额。你可以先用免费额度测试，跑通了再决定充不充。

| 模型 | [千聚api聚合站](https://www.qianjuai.com/)费率 | 调用成本（每百万 Token 输入） |
|--------|-----------------------|--------------------------------|
| glm-4-plus | 官方 ×1 | ≈ 0.5 元 |
| glm-4 | 官方 ×1 | ≈ 0.15 元 |
| glm-4-9b-chat | 官方 ×0.6 | ≈ 0.08 元 |
| GLM-4V（多模态视觉） | 官方 ×1.2 | ≈ 0.6 元 |

👉 [注册查看完整费率表](https://www.qianjuai.com/register)

---

### 更高级的用法：流式输出和多轮对话

上面演示的是最简单的请求-响应模式。如果你想实现那种“一个字一个字往外蹦”的流式输出（ChatGPT 的体验），代码改动也很小：

javascript
const stream = await client.chat.completions.create({
  model: 'glm-4-plus',
  messages: [{ role: 'user', content: '写一首关于秋天的五言绝句。' }],
  stream: true,                            // 开启流式输出
});

for await (const chunk of stream) {
  process.stdout.write(chunk.choices[0]?.delta?.content || '');
}


同样只需要改 base_url 和 api_key，其他代码完全不用动。多轮对话也一样，把 `messages` 数组里加上 `role: 'assistant'` 的历史回复就行，这里就不展开了。

---

### 在常用工具里直接接智谱清言

[千聚api聚合站](https://www.qianjuai.com/)的接口除了能写在 Node.js 代码里，还可以直接配置到很多 AI 工具客户端中，让你在聊天工具或 IDE 里用上智谱清言的能力：

- **Cursor / Windsurf**：在设置中找到 API Endpoint，填入 `https://www.qianjuai.com/v1`，模型名填 `glm-4-plus`，代码补全和聊天体验非常丝滑。
- **LobeChat**：添加自定义模型提供商，同样配置 base_url 和 api_key，立刻能用智谱清言对话。
- **沉浸式翻译**：自定义 API 来源，填入[千聚api聚合站](https://www.qianjuai.com/)地址，翻译质量显著提升。

这些工具的配置界面都有清晰的截图指引，在[千聚api聚合站](https://www.qianjuai.com/)官方文档里可以找到。

---

### 新手常见问题排查

如果你在跑代码时遇到报错，99% 的情况是以下三个原因之一：

1. **API key 填错了**：复制时注意不要有多余空格，或者重新生成一个试试。
2. **模型名拼写错误**：GLM-4 系列的模型名是 `glm-4-plus`、`glm-4`、`glm-4-9b-chat`，注意大小写和短横线的正确格式。
3. **余额不足**：新用户的 $0.2 额度足够测试几十次。如果不够用，最低充 1 元就能继续。

如果这些都没问题，可以去控制台查看请求日志，那里会显示每一次调用的状态码和错误详情，比盲猜有效率得多。

---

### 适合哪些人用这篇教程

这个方案对三类人特别友好：

**新手开发者**：刚学 Node.js，想试试 AI API 调用，不想被翻墙和绑卡劝退，沿着上面的步骤走就行了。

**个人项目玩家**：想给自己的小工具或自动化脚本加点 AI 能力，用最简单的方式接入智谱清言，不折腾环境。

**需要快速验证的团队**：在做产品原型或 POC 阶段，用[千聚api聚合站](https://www.qianjuai.com/)一把梭，调通了再考虑是否迁移到自己部署的模型服务。

---

### 总结：零门槛、免翻墙、一次跑通

今天我们做了一件事：在一个全新的 Node.js 项目里，只装了 `openai` 一个依赖包，只改了两行配置（base_url 和 api_key），就用代码成功调用了智谱清言的 GLM-4-Plus 模型。

整个过程没有翻墙、没有绑海外信用卡、没有安装额外的代理工具，真正做到了“新手也能一次跑通”。智谱清言作为国产大模型里能力靠前的产品，在语言理解、代码生成和逻辑推理上的表现有目共睹，通过[千聚api聚合站](https://www.qianjuai.com/)这种低代码的方式接入，等于给你省去了最麻烦的那段路。

👉 [立即注册千聚api聚合站，免翻墙调用智谱清言](https://www.qianjuai.com/register)